<Window x:Class="Tuft.ui.UiShowLists"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tuft.ui"
        mc:Ignorable="d"
        ScrollViewer.VerticalScrollBarVisibility="Visible"
        
        Title="UiShowLists" Height="300" Width="300">

    <ScrollViewer  Margin="0,1,0,0" CanContentScroll="True" SnapsToDevicePixels="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" PanningMode="HorizontalOnly" DataContext="{Binding}">
        <StackPanel  x:Name="stack_items" VerticalAlignment="Top"   Orientation="Vertical" CanVerticallyScroll="True" CanHorizontallyScroll="True"  ForceCursor="False" SnapsToDevicePixels="True" OverridesDefaultStyle="True"/>
    </ScrollViewer>
   
</Window>
